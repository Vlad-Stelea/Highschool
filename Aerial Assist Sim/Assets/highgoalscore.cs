using UnityEngine;
using System.Collections;

public class highgoalscore : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void OnCollisionEnter (Collision collison) {
		if (collison.gameObject.name == ("blue ball")){



		}

	
	}
}
